apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefulset
spec:
  template:
    spec:
      volumes:
        - name: meili-index
          persistentVolumeClaim:
            claimName: search-pvc