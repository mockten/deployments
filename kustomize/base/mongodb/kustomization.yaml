apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: mongo
namePrefix: mongo-

resources:
- mongo-deploy.yaml
- mongo-service.yaml
- mongo-secret.yaml
- init-config.yaml